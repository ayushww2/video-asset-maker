export default function HomePage() {
  return (
    <main className="relative z-10 mx-auto flex min-h-full w-full max-w-2xl flex-1 flex-col justify-center px-6 py-16">
      <p className="font-[family-name:var(--font-ibm-plex-mono)] text-[0.7rem] uppercase tracking-[0.18em] text-[var(--accent)]">
        Railway
      </p>
      <h1 className="mt-3 text-5xl font-semibold tracking-tight">Video Asset Maker</h1>
      <p className="mt-4 max-w-md text-[var(--muted)]">Empty. Unique work goes here.</p>
    </main>
  );
}
